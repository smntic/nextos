{ ... }:

# Returns a string containing the config to place BEFORE everything else in the init.lua file.
''
  -- Set leader to space (must be defined BEFORE mappings with <leader>)
  vim.g.mapleader = ' '
''

